% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior-density-ordinate.R
\name{prior_density_ordinate}
\alias{prior_density_ordinate}
\title{Classify a prior-density ordinate}
\usage{
prior_density_ordinate(x, value)
}
\arguments{
\item{x}{A BayesTools prior object or a \code{prior_linear_density} object produced
by BayesTools' deterministic prior-density builders.}

\item{value}{One finite, non-missing numeric value at which to classify the
prior ordinate.}
}
\value{
A named list of class \code{prior_density_ordinate} with fields:
\itemize{
\item \code{schema_version}: the stable result-schema version, currently \code{"1"};
\item \code{value}: the requested value;
\item \code{behavior}: one of \code{"regular"}, \code{"zero"}, \code{"infinite"},
\code{"point_mass"}, \code{"undefined"}, or \code{"unknown"};
\item \code{log_density}: the continuous log-density ordinate, when available;
\item \code{point_mass}: discrete probability at exactly \code{value};
\item \code{exact}: whether \code{behavior} follows from deterministic prior provenance;
\item \code{method}: a machine-readable classification method;
\item \code{reason}: \code{NULL} for an ordinary regular ordinate and a concise diagnostic
otherwise;
\item \code{provenance}: compact deterministic information used for classification.
}

A \code{regular} classification is structural. Consequently, \code{log_density} may
be \code{-Inf} when an otherwise positive finite density underflows in ordinary
floating-point evaluation. General numerical convolutions, products, and
arbitrary user transformations are reported as \code{unknown} unless exact point
mass or singular-point metadata establishes the requested behavior.
}
\description{
\code{prior_density_ordinate()} classifies the mathematical behavior of a prior at
one exact value. The classification uses the prior definition and
deterministic provenance recorded on linear-combination prior densities. It
does not use samples, kernel density estimates, numerical grids, or nearby
probe values to establish the behavior.
}
\examples{
normal_prior <- prior("normal", list(mean = 0, sd = 1))
prior_density_ordinate(normal_prior, 0)

gamma_prior <- prior("gamma", list(shape = 0.5, rate = 1))
prior_density_ordinate(gamma_prior, 0)

mixture_prior <- prior_mixture(
  list(
    prior("point", list(location = 0), prior_weights = 1),
    prior("normal", list(mean = 0, sd = 1), prior_weights = 3)
  ),
  is_null = c(TRUE, FALSE)
)
prior_density_ordinate(mixture_prior, 0)

}
