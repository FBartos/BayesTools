% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JAGS-formula-predict-target.R
\name{JAGS_predict_formula}
\alias{JAGS_predict_formula}
\title{Formula prediction with explicit random-effect target}
\usage{
JAGS_predict_formula(
  fit,
  parameter,
  formula = NULL,
  data = NULL,
  prior_list = NULL,
  formula_target = c("conditional", "fixed", "marginal"),
  blocks = NULL,
  new_levels = NULL,
  marginal_method = c("covariance", "sample"),
  seed = NULL,
  components = FALSE,
  fitted_rows = NULL
)
}
\arguments{
\item{fit}{model fitted with \code{\link[=JAGS_fit]{JAGS_fit()}} or posterior samples carrying
\code{formula_design} metadata.}

\item{parameter}{formula parameter name.}

\item{formula}{optional formula. If \code{NULL}, the fitted formula for
\code{parameter} is used.}

\item{data}{optional prediction data. If \code{NULL}, fitted source data are used.}

\item{prior_list}{optional named prior list. If \code{NULL}, fitted priors are
used.}

\item{formula_target}{prediction target: \code{"conditional"}, \code{"fixed"}, or
\code{"marginal"}. The default includes fitted random-effect contributions.}

\item{blocks}{optional random-effect block names.}

\item{new_levels}{new-level policy for conditional or marginal random-effect
prediction. Use a \code{random_new_levels()} object or one of \code{"error"}, \code{"zero"},
or \code{"sample"}.}

\item{marginal_method}{marginal representation used only with
\code{formula_target = "marginal"}: \code{"covariance"} returns fixed means plus
marginal random-effect covariance; \code{"sample"} returns materialized
random-effect draws. For known group covariance, \code{"sample"} jointly draws
the requested fitted levels from the corresponding covariance submatrix;
new levels remain unsupported.}

\item{seed}{optional random seed used when random-effect draws are simulated.}

\item{components}{whether to include component matrices where available.}

\item{fitted_rows}{optional integer vector mapping supplied prediction rows
to fitted observation indices. It is required for selected
posterior-indexed row sources. Reordering and duplicate indices are
supported; callback-computed row sources do not use it.}
}
\value{
A list of class \code{BayesTools_formula_prediction} with fields \code{value},
\code{mean}, \code{random}, \code{vcov}, \code{components}, and \code{metadata}.
}
\description{
Returns formula-scale predictions from fitted BayesTools formula metadata.
Unlike \code{\link[=JAGS_evaluate_formula]{JAGS_evaluate_formula()}}, this helper can return structured
prediction output for marginal random-effect targets, including marginal
random-effect covariance or simulated random-effect draws.
}
\seealso{
\code{\link[=JAGS_evaluate_formula]{JAGS_evaluate_formula()}} \code{\link[=random_effects_marginal_vcov]{random_effects_marginal_vcov()}}
}
