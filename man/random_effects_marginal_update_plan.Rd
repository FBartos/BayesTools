% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-effects-marginal-update.R
\name{random_effects_marginal_update_plan}
\alias{random_effects_marginal_update_plan}
\title{Random-effect marginal covariance update plan}
\usage{
random_effects_marginal_update_plan(fit, selection)
}
\arguments{
\item{fit}{a fitted object carrying a BayesTools parameter map and formula
design metadata.}

\item{selection}{a one-quantity selection returned by
\code{\link[=parameter_catalog_resolve]{parameter_catalog_resolve()}}.}
}
\value{
A list of class
\code{BayesTools_random_effects_marginal_update_plan}. Field \code{family} is one of
\code{"affine"}, \code{"factor"}, \code{"markov"}, or \code{"unsupported"}. An affine plan
additionally records the scalar covariance coefficient transform and the
affected random-effect blocks.
}
\description{
Resolves a selected public random-effect quantity to the exact scalar
dependence of its marginal covariance contribution. The result is compiled
exclusively from the fitted parameter map and formula random-effect
metadata; posterior draws are neither inspected nor used to infer an update
form.
}
\details{
\code{family = "affine"} means that, conditional on every non-selected posterior
coordinate, the row covariance has the exact form \code{A + h(value) * B}, where
\code{value} is the selected quantity's \code{source_parameter} coordinate and \code{h} is
recorded in \code{coefficient_transform}.
\code{family = "factor"} identifies an exact candidate-dependent covariance
factor, while \code{family = "markov"} identifies a compiled structured Markov
state. Unsupported plans carry a structural reason. The accessor never
estimates affineness from evaluated covariance matrices.
}
\seealso{
\code{\link[=parameter_catalog]{parameter_catalog()}} \code{\link[=random_effects_marginal_factor_states]{random_effects_marginal_factor_states()}}
}
