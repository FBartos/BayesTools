% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JAGS-parameter-registry.R
\name{JAGS_parameter_registry}
\alias{JAGS_parameter_registry}
\alias{JAGS_parameter_registry_schema}
\title{Canonical JAGS parameter registry}
\usage{
JAGS_parameter_registry(fit)

JAGS_parameter_registry_schema()
}
\arguments{
\item{fit}{a fitted object created by \code{\link[=JAGS_fit]{JAGS_fit()}}.}
}
\value{
\code{JAGS_parameter_registry()} returns a
\code{BayesTools_parameter_registry} data frame.
\code{JAGS_parameter_registry_schema()} returns a data frame describing each
registry field.
}
\description{
\code{JAGS_parameter_registry()} returns the versioned parameter registry stored
by \code{\link[=JAGS_fit]{JAGS_fit()}}. The registry is the authoritative mapping between concrete
posterior columns and their formula, random-effect block, semantic role,
fitted coordinate scale, and display label. Downstream packages should use
this accessor instead of parsing JAGS parameter names.

\code{JAGS_parameter_registry_schema()} documents the stable fields in the
current registry schema. Registry rows are unique by \code{canonical_name}.
Matrix/vector indices are stored as comma-separated text in \code{index}, and
their fitted dimensions are stored in \code{dimensions} (for example, \code{"3x2"}).
Empty strings mean that a field does not apply.
Structural point-prior rows store the exact prior-specified value in
\code{fixed_value}; sampled and unavailable rows store \code{NA_real_}.

Raw random-effect latent variables, realized group coefficients, and
spike-and-slab implementation coordinates have \code{internal = TRUE}. Latent
and realized coefficients use the \code{"unit_latent"} and
\code{"fitted_standardized"} scales, respectively. Spike-and-slab component
indicators and inclusion probabilities are \code{"unitless"}, while their slab
SD variables use \code{"fitted_covariance"}. These coordinates must not be
presented as original-scale coefficients.
}
