% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-priors.R
\name{prior_random}
\alias{prior_random}
\alias{random_block}
\alias{random_term}
\alias{random_variance_allocation}
\alias{allocation_ref}
\alias{random_covariance}
\alias{prior_lkj}
\alias{random_monitor}
\alias{random_new_levels}
\title{Random-effect prior specification}
\usage{
prior_random(
  ...,
  sd = NULL,
  covariance = NULL,
  cor = NULL,
  rho = NULL,
  monitor = random_monitor(),
  new_levels = random_new_levels(),
  allocation = NULL,
  parameterization = "noncentered"
)

random_block(
  sd = NULL,
  covariance = NULL,
  cor = NULL,
  rho = NULL,
  monitor = NULL,
  new_levels = NULL,
  terms = NULL,
  sd_source = NULL,
  parameterization = NULL
)

random_term(
  sd = NULL,
  covariance = NULL,
  cor = NULL,
  rho = NULL,
  monitor = NULL,
  new_levels = NULL,
  terms = NULL,
  sd_source = NULL,
  parameterization = NULL
)

random_variance_allocation(
  terms = NULL,
  sd = NULL,
  weights = NULL,
  name = NULL,
  parent = NULL,
  target = c("block", "sd_component"),
  scale = c("total_variance", "mean_variance"),
  sd_source = NULL,
  inclusion = NULL
)

allocation_ref(allocation_name, component)

random_covariance(
  structure = NULL,
  sd = NULL,
  cor = NULL,
  rho = NULL,
  eta = NULL,
  rho_scale = c("fisher_z", "rho", "logit")
)

prior_lkj(eta = 1, include_correlation = TRUE, include_primitives = FALSE)

random_monitor(
  latent = TRUE,
  coefficients = FALSE,
  correlation = TRUE,
  lkj_primitives = FALSE
)

random_new_levels(method = c("error", "zero", "sample"))
}
\arguments{
\item{...}{named \code{random_block()} specifications and, optionally,
top-level \code{random_variance_allocation()} specifications.}

\item{sd}{prior distribution for random-effect standard deviations.}

\item{covariance}{optional \code{random_covariance()} specification.}

\item{cor}{optional correlation prior. Use \code{prior_lkj()} for unstructured
correlation matrices.}

\item{rho}{optional scalar correlation prior for structured correlation
models. The default scale is Fisher's z.}

\item{monitor}{\code{random_monitor()} object.}

\item{new_levels}{\code{random_new_levels()} object controlling prediction for
previously unseen grouping levels.}

\item{allocation}{optional \code{random_variance_allocation()} specification, or
a list of such specifications, defining total-SD plus Dirichlet variance
allocation across named random-effect blocks.}

\item{parameterization}{requested sampled random-effect parameterization:
\code{"noncentered"}, \code{"centered"}, or \code{"auto"}. The top-level value is
inherited by blocks without an explicit \code{random_block()} override.}

\item{terms}{for \code{random_block()}, an optional named list of term-specific
SD overrides. Each entry must be either a prior object or
\code{random_block(sd = ...)}, and names must match resolved model terms such
as \code{"intercept"} or a slope term label. For
\code{random_variance_allocation()}, optional random-effect block names targeted
by the shared allocation. If \code{NULL}, the allocation targets all remaining
unallocated random-effect blocks at resolution time.}

\item{sd_source}{for \code{random_block()}, optional external random-effect SD
source created with \code{random_sd_source()}. A block-local \code{sd_source}
replaces inherited top-level SD priors and inherited covariance SD priors,
but conflicts with block-local \code{sd}, block-local \code{covariance$sd}, and
term-specific SD overrides. For \code{random_variance_allocation()}, root
allocations must specify exactly one of \code{sd} or \code{sd_source}; child
allocations inherit their SD budget from \code{parent}.}

\item{weights}{optional Dirichlet simplex prior over allocation weights.}

\item{name}{optional allocation label. Required when another allocation uses
this allocation as \code{parent}.}

\item{parent}{optional \code{allocation_ref()} object selecting a component of an
earlier named allocation. Child allocations inherit that component's SD
budget and must not specify \code{sd} or \code{sd_source}.}

\item{target}{allocation target type. \code{"block"} allocates across named
random-effect blocks or symbolic intermediate components.
\code{"sd_component"} expands one selected heterogeneous block to its resolved
SD components.}

\item{scale}{variance normalization. \code{"total_variance"} uses
\code{sd_child = sd_parent * sqrt(w)}. \code{"mean_variance"} uses
\code{sd_child = sd_parent * sqrt(K * w)} and is intended for
\code{target = "sd_component"} heterogeneity tests.
Summaries report \code{w} as a variance fraction for \code{"total_variance"} and
\code{K * w} as a variance ratio to the average SD-component variance for
\code{"mean_variance"}.}

\item{inclusion}{optional named list of scalar probability priors, keyed by
allocation component label. For \code{target = "block"}, each listed component
receives an independent Bernoulli gate and its SD contribution is multiplied
by that gate. Gate names must match resolved allocation component labels.}

\item{allocation_name}{allocation name referenced by \code{allocation_ref()}.}

\item{component}{component label inside the referenced allocation. Named
\code{terms} supply component labels; unnamed \code{terms} use sanitized term names.}

\item{structure}{covariance structure label. Accepted labels are \code{"ID"},
\code{"DIAG"}, \code{"CS"}, \code{"HCS"}, \code{"AR"} / \code{"AR1"}, \code{"HAR"}, \code{"CAR"}, and
\code{"UN"} / \code{"US"}; matching is case-insensitive.}

\item{eta}{LKJ concentration parameter used when \code{cor} is omitted for
unstructured covariance. Values above one regularize correlations toward
zero; \code{eta = 1} is uniform over correlation matrices.}

\item{rho_scale}{scale for scalar correlation priors. \code{"fisher_z"} is the
default, \code{"logit"} uses a logit transform of the valid raw-rho interval,
and \code{"rho"} uses the raw correlation parameter.}

\item{include_correlation}{whether to monitor the correlation matrix for
\code{prior_lkj()}.}

\item{include_primitives}{whether to monitor LKJ primitive beta coordinates
for \code{prior_lkj()}.}

\item{latent}{whether to monitor standardized latent random effects.
Required for bridge sampling and coefficient reconstruction.}

\item{coefficients}{whether to monitor realized group-level coefficients.
This is convenient for inspection but can be memory intensive.}

\item{correlation}{whether correlation matrices are part of the semantic
monitored output. Scalar CS/HCS/AR1/HAR/CAR backends retain only \code{rho}
during sampling and derive the requested matrix afterward; this avoids a
quadratic JAGS graph without changing the correlation target.}

\item{lkj_primitives}{whether to monitor user-facing LKJ primitive beta
coordinates. For formula \code{us} random effects, raw LKJ primitive \code{u}
coordinates are always retained internally because bridge sampling evaluates
the LKJ prior on those coordinates.}

\item{method}{new-level prediction method: \code{"error"} disallows new levels,
\code{"zero"} fixes their random-effect contribution at zero, and \code{"sample"}
treats them as draws from the fitted random-effect distribution.}
}
\value{
A list-like S3 object describing random-effect priors, covariance
settings, allocation settings, and monitoring policy.
}
\description{
\code{prior_random()} defines priors and policies for formula random effects.
Random-effect formulas own the grouping variables, slopes, intercepts, and
covariance structure. \code{prior_random()} supplies priors and monitoring policy
for the already parsed random-effect blocks.
Random-effect grouping expressions must be grouping variables, explicit \code{:}
interactions, or explicit \code{/} nesting. Derived grouping variables should be
materialized as columns in \code{data} before they are referenced by a formula.
}
\details{
Formula random effects in \code{\link[=JAGS_formula]{JAGS_formula()}} and \code{\link[=JAGS_fit]{JAGS_fit()}} require an
explicit \code{prior_random()} object. The top-level arguments are defaults used
by every random-effect block unless a named \code{random_block()} override is
supplied in \code{...}.

Each override name must match a stable random-effect block label generated
from the formula parser or supplied by the user through named random terms.
This keeps prior specification separate from formula syntax. For example,
if the formula contains a block named \code{"study"}, then
\code{prior_random(study = random_block(...))} overrides only that block.

\code{random_term()} is an alias of \code{random_block()} retained for readability in
user-facing code.
}
\section{Standard-deviation priors}{

\code{sd} must be a BayesTools prior object with nonnegative support, such as a
truncated normal, gamma, point prior at zero, or mixture prior. When a
structure needs multiple marginal standard deviations, a single \code{sd} prior is
replicated over those SD components unless a more specific override is
supplied by the random-effect resolver.

For ordinary estimation, use continuous positive SD priors. Use point or
mixture priors at zero only when a random-effect component is intentionally
fixed or model-averaged.
}

\section{Covariance structures}{

\code{random_covariance()} stores structure-specific covariance settings.
Supported structure labels are:
\itemize{
\item \code{"ID"}: independent columns with one shared SD.
\item \code{"DIAG"}: independent columns with one SD per generated column.
\item \code{"CS"} / \code{"HCS"}: compound symmetry over one or more index variables,
with one scalar correlation \code{rho}; \code{"HCS"} has one SD per index level.
Multiple index variables in \code{cs(index1 + index2 | group)} are combined
with \code{interaction(..., drop = TRUE)}.
\item \code{"AR"} / \code{"AR1"} / \code{"HAR"}: discrete autoregressive structure over a
single ordered index variable, with one scalar correlation \code{rho}; \code{"HAR"}
has one SD per index level.
\item \code{"CAR"}: continuous-time AR(1), currently for \code{car(time | group)},
using \code{rho ^ abs(t_i - t_j)} with \verb{0 <= rho < 1}.
\item \code{"UN"} / \code{"US"}: unstructured covariance with SD priors and \code{prior_lkj()}.
}

The formula parser owns the actual structure used by a block. Supplying a
conflicting \code{structure} in \code{random_covariance()} is rejected when the formula
is resolved.
}

\section{Correlation priors}{

Use \code{prior_lkj(eta = ...)} for full unstructured correlation matrices. LKJ
priors use the BayesTools compiled JAGS module.

Use \code{rho} for scalar-correlation structures (\code{"CS"}, \code{"HCS"}, \code{"AR"},
\code{"HAR"}, and \code{"CAR"}). \code{rho_scale} controls the scale on which the prior is
placed:
\itemize{
\item \code{"fisher_z"} places the prior on \code{atanh(rho)} and is the default. A point
prior at zero corresponds to independence. For one-sided structures such
as \code{"CAR"}, the Fisher-z prior is truncated to the valid raw-rho interval.
\item \code{"logit"} places the prior on a logit transform of the valid raw-rho
interval.
\item \code{"rho"} places the prior directly on raw \code{rho}; the prior is truncated to
the structure-specific valid interval.
}
}

\section{Variance allocation}{

\code{random_variance_allocation()} expresses one total SD source plus a Dirichlet
prior over variance fractions. Root allocations must specify exactly one of
a prior-owned \code{sd} prior or \code{sd_source = random_sd_source(...)}.
\code{sd_source} can reference an already-defined JAGS node such as scalar \code{tau}
or row-shaped \code{tau} without creating a prior for that node. Multiple named
allocations can be supplied in
\code{prior_random()}; child allocations can inherit one component of an earlier
allocation through \code{parent = allocation_ref(...)}.

\deqn{\sigma_j = \sigma_{\mathrm{total}}\sqrt{w_j}, \quad
      w \sim \mathrm{Dirichlet}(\alpha).}

This is useful for nested or crossed random intercepts when the prior should
control total heterogeneity separately from how that heterogeneity is
allocated across components. The allocation targets named random-effect
blocks through \code{terms}. If \code{terms = NULL}, a single root block allocation
targets all random-effect blocks at resolution time and must resolve to at
least two blocks. Generated JAGS
syntax supports allocated blocks with one SD component, homogeneous
structures where one SD controls the block, and explicit
\code{target = "sd_component"} allocations that split the resolved SD components
of one heterogeneous block. Row-shaped external sources are applied inside
the observation-level random-effect contribution, including child
allocations and SD-component allocations; they do not create scalar SD
summary parameters. Prediction and bridge-sampling reconstruction for these
sources require standardized latent random-effect monitors, row-source values
from either posterior columns or a \code{parameter_source(..., values = ...)}
callback, and any required Dirichlet allocation coordinates.
Use \code{scale = "total_variance"} to preserve summed variance and
\code{scale = "mean_variance"} to preserve average variance inside a
heterogeneous block.
}

\section{Monitoring and prediction}{

\code{random_monitor()} controls which random-effect quantities are saved from
JAGS. The default monitors standardized latent random effects because they
are required for bridge sampling and for reconstructing group-level
coefficients in prediction. Turning \code{latent = FALSE} can reduce memory use,
but bridge sampling and conditional prediction may require refitting.

\code{random_new_levels()} controls prediction for grouping levels that were not
observed when the model was fitted. The default \code{method = "error"} disallows
new levels. \code{method = "zero"} assigns zero random-effect contribution to new
levels and \code{method = "sample"} draws new group-level coefficients from the
fitted random-effect distribution during draw-valued prediction or integrates
them through the block covariance for covariance-valued prediction.
}

\section{Parameterization}{

\code{parameterization} controls how sampled random-effect blocks are represented
by the computational backend. \code{"noncentered"} retains standardized latent
effects, \code{"centered"} samples realized group-level coefficients, and
\code{"auto"} lets the backend choose a supported representation from the
resolved block design. Its deterministic heuristic uses within-group
replication and design conditioning; it does not inspect outcome-specific
likelihood curvature. The choice does not change the SD, allocation, or
correlation priors. Block-specific values supplied through \code{random_block()}
override the top-level default. Explicit \code{"centered"} compilation requires
strictly positive backend-owned SD coordinates and therefore rejects
point-mass-at-zero scales, inclusion-gated variance allocations, and external
or row-indexed SD sources. \code{"auto"} falls back to \code{"noncentered"} when those
contracts or its design-information checks do not support centering.
}

\examples{
sd_prior <- prior(
  "normal", list(mean = 0, sd = 0.5),
  truncation = list(lower = 0, upper = Inf)
)
rho_prior <- prior("normal", list(mean = 0, sd = 0.5))

# One SD prior used for every random-effect block.
prior_random(sd = sd_prior)

# Override a named block while keeping a global default.
prior_random(
  sd = sd_prior,
  study = random_block(sd = prior("gamma", list(shape = 2, rate = 2)))
)

# Unstructured covariance: SD prior plus LKJ correlation prior.
prior_random(
  study = random_term(
    covariance = random_covariance(
      structure = "UN",
      sd = sd_prior,
      cor = prior_lkj(eta = 2)
    )
  )
)

# Scalar-rho structure with a Fisher-z prior.
prior_random(
  study_time = random_term(
    covariance = random_covariance(
      structure = "AR",
      sd = sd_prior,
      rho = rho_prior,
      rho_scale = "fisher_z"
    )
  )
)

# Total SD plus Dirichlet variance allocation.
prior_random(
  random_variance_allocation(
    terms = c("study", "drug"),
    sd = sd_prior,
    weights = prior("dirichlet", list(alpha = c(1, 1)))
  )
)

# A child allocation can split one inherited variance budget.
prior_random(
  random_variance_allocation(
    name = "total_re",
    terms = c(nested = "nested", simple = "study"),
    sd = sd_prior,
    weights = prior("dirichlet", list(alpha = c(1, 1)))
  ),
  random_variance_allocation(
    name = "nested_split",
    parent = allocation_ref("total_re", "nested"),
    terms = c("paper", "country"),
    weights = prior("dirichlet", list(alpha = c(3, 1)))
  )
)
}
