% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-effects-marginal-covariance.R
\name{random_effects_marginal_variance_factors}
\alias{random_effects_marginal_variance_factors}
\title{Random-effect marginal variance factors}
\usage{
random_effects_marginal_variance_factors(
  formula_design,
  blocks = NULL,
  require_diagonal = TRUE,
  require_one_to_one = FALSE
)
}
\arguments{
\item{formula_design}{a \code{BayesTools_formula_design} object returned by
\code{\link[=JAGS_formula]{JAGS_formula()}} or \code{\link[=JAGS_formula_design]{JAGS_formula_design()}}.}

\item{blocks}{optional character vector of random-effect block names. When
\code{NULL}, only marginalized random-effect blocks are selected.}

\item{require_diagonal}{whether to require the row-space covariance factor to
be diagonal. This protects downstream likelihoods that can only consume
row-wise variance multipliers.}

\item{require_one_to_one}{whether each observation row must map to a distinct
grouping level.}
}
\value{
A list of class
\code{BayesTools_random_effects_marginal_variance_factors} with selected block
metadata. Each block contains \code{row_multiplier}, \code{group_map},
\code{group_levels}, \code{model_matrix}, SD metadata, compile mode, and known group
covariance metadata when present.
}
\description{
Extracts row-aligned design multipliers for one-column formula random-effect
blocks that are intended to be marginalized by a downstream likelihood. For
a block with SD parameter \eqn{\tau}, the marginal variance contribution is
\eqn{\tau^2} times \code{row_multiplier}. With known group covariance this factor
is built from the prepared group kernel, so the dense row-space covariance
factor is \eqn{Z K Z'}.
}
\details{
Unsupported factor representations signal an error inheriting from
\code{BayesTools_random_effects_marginal_variance_unavailable}. The condition
carries stable \code{block_name} and \code{reason} fields so downstream packages can
distinguish structural unavailability from invalid inputs. \code{reason} is one
of \code{"row_indexed_sd"}, \code{"multiple_columns"},
\code{"non_diagonal_row_covariance"}, or \code{"repeated_groups"}.
}
\seealso{
\code{\link[=random_effects_marginal_vcov]{random_effects_marginal_vcov()}} \code{\link[=random_effects_compile]{random_effects_compile()}}
}
