% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JAGS-formula-predictor-basis.R
\name{JAGS_formula_predictor_basis}
\alias{JAGS_formula_predictor_basis}
\title{Exact fitted-formula predictor update basis}
\usage{
JAGS_formula_predictor_basis(fit, directions, posterior_samples = NULL)
}
\arguments{
\item{fit}{fitted object created by \code{\link[=JAGS_fit]{JAGS_fit()}}.}

\item{directions}{a fully named numeric vector, or a numeric matrix with
unique coefficient-coordinate column names. Matrix rows define separate
posterior-row update directions.}

\item{posterior_samples}{optional posterior sample matrix aligned with the
rows of \code{directions}. It is required when a selected formula term has a
parameter-valued \code{multiply_by} attribute; when omitted, draws are obtained
from \code{fit}.}
}
\value{
A \code{BayesTools_formula_predictor_basis} list with \code{status}, \code{reason},
formula \code{parameter}, selected \code{coordinates}, and an \code{S} by \code{N} numeric
\code{basis}. \code{basis} is present only when \code{status} is \code{"affine"}.
}
\description{
\code{JAGS_formula_predictor_basis()} uses the fitted parameter map and persisted
formula design to construct the exact observation-level change in a fixed
linear predictor for a supplied direction through fitted coefficient
coordinates. The result is conditional on all coordinates outside that
direction, including fitted random effects.

The accessor reports \code{"affine"} only when the stored metadata proves that
the selected update is additive on the fitted predictor scale. Logged
intercepts and coordinates reused by formula expressions, coefficient
multipliers, or random-effect scale sources are reported as non-affine or
unsupported so callers can retain a generic evaluator.
}
\seealso{
\code{\link[=JAGS_formula_design]{JAGS_formula_design()}} \code{\link[=parameter_map]{parameter_map()}}
}
