% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-effects-correlation-draws.R
\name{random_effects_correlation_draws}
\alias{random_effects_correlation_draws}
\title{Scalar-structured random-effect correlation draws}
\usage{
random_effects_correlation_draws(random_term, posterior_samples)
}
\arguments{
\item{random_term}{compiled random-effect term metadata from a
\code{BayesTools_formula_design} object.}

\item{posterior_samples}{posterior sample matrix, data frame, \code{mcmc}, or
\code{mcmc.list}. Samples must contain the compiled internal scalar-correlation
coordinate unless it is fixed in \code{random_term} metadata. This coordinate
is a backend dependency, not the public semantic \code{cor} quantity. A
one-coefficient block needs no correlation coordinate and accepts a numeric
draw-by-zero matrix.}
}
\value{
A dense numeric array with dimensions
\verb{draw x coefficient x coefficient}. Coefficient dimension names use the
compiled random-effect column names when available.
}
\description{
Reconstructs dense correlation matrices for one scalar-structured formula
random-effect block from its compiled metadata and posterior draws. The
matrices are derived only when this function is called; fitted objects can
otherwise retain the compact scalar correlation coordinate.
}
\details{
Supported structures are compound symmetry (\code{"CS"} and \code{"HCS"}), discrete
autoregressive (\code{"AR1"} and \code{"HAR"}), and continuous-time autoregressive
(\code{"CAR"}). Correlation coordinates are transformed and checked against the
structure-specific support recorded in \code{random_term}. \code{"CAR"} matrices use
the compact ordered time coordinates stored by the formula compiler.

The returned array is dense and therefore requires memory proportional to
the number of posterior draws times the squared coefficient count.
}
\examples{
dat <- data.frame(
  study = factor(rep(c("s1", "s2"), each = 3)),
  time = factor(rep(1:3, 2))
)
generated <- JAGS_formula(
  ~ 1 + ar1(time | study),
  parameter = "mu",
  data = dat,
  prior_list = list(intercept = prior("normal", list(0, 1))),
  prior_random = prior_random(
    sd = prior("point", list(location = 1)),
    cor = prior("normal", list(0, 0.5))
  )
)
random_term <- generated$formula_design$random_effects[[1]]
posterior <- matrix(c(-0.2, 0.4), ncol = 1)
colnames(posterior) <- random_term$correlation$rho_name
random_effects_correlation_draws(random_term, posterior)

}
\seealso{
\code{\link[=prior_random]{prior_random()}} \code{\link[=random_effects_marginal_vcov]{random_effects_marginal_vcov()}}
}
