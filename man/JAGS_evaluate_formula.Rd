% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JAGS-formula-predict.R
\name{JAGS_evaluate_formula}
\alias{JAGS_evaluate_formula}
\title{Evaluate JAGS formula using posterior samples}
\usage{
JAGS_evaluate_formula(
  fit,
  formula = NULL,
  parameter,
  data = NULL,
  prior_list = NULL,
  formula_target = NULL,
  blocks = NULL,
  new_levels = NULL
)
}
\arguments{
\item{fit}{model fitted with either \link[runjags]{runjags} posterior
samples obtained with \link[rjags]{rjags-package}}

\item{formula}{formula specifying the right hand side of the assignment (the
left hand side is ignored). If \code{NULL}, the fitted formula stored in
\code{formula_design} metadata is used. If the formula has a
\code{"log(intercept)"} attribute set to \code{TRUE}, the intercept values
will be log-transformed before computing the linear predictor.}

\item{parameter}{name of the parameter created with the formula}

\item{data}{data.frame containing predictors included in the formula. If
\code{NULL}, fitted source data from \code{formula_design} metadata are used.}

\item{prior_list}{named list of prior distribution of parameters specified
within the \code{formula}. If \code{NULL}, fitted priors from \code{formula_design}
metadata are used.}

\item{formula_target}{optional formula prediction target. \code{NULL} preserves
the historical safety behavior. \code{"fixed"} evaluates only the fixed formula
contribution. \code{"conditional"} evaluates fixed effects plus fitted or
explicitly generated random-effect contributions.}

\item{blocks}{optional random-effect block names used with
\code{formula_target = "conditional"}.}

\item{new_levels}{optional new-level policy used only with
\code{formula_target = "conditional"}. Use a \code{random_new_levels()} object or one
of \code{"error"}, \code{"zero"}, or \code{"sample"}.}
}
\value{
\code{JAGS_evaluate_formula} returns a matrix of the evaluated posterior samples on
the supplied data.
}
\description{
Evaluates a JAGS formula on a posterior distribution obtained
from a fitted model. Formula random effects can be evaluated for existing
grouping levels when either standardized latent random effects and covariance
hyperparameters were monitored via \code{random_monitor(latent = TRUE)}, or
the group-level coefficients were monitored via
\code{random_monitor(coefficients = TRUE)}.
Row-indexed external random-effect SD sources, such as
\code{random_sd_source("tau", shape = "row")}, are evaluated from latent
random effects only. Model generation automatically monitors the required
latent effects for these blocks. The posterior samples must either contain
source columns named \code{tau[1]}, ..., \code{tau[nrow(data)]} aligned to
the supplied prediction rows, or the source must provide a
\code{parameter_source()} \code{values} function for reconstructing row-wise
source values from the posterior samples and supplied prediction data.
Literal \code{expression()} terms cannot be reconstructed automatically.
Replaying a fitted formula that contains them produces an error rather than
silently omitting their contribution. An explicit expression-free formula
can still be supplied to evaluate a selected subset of the fitted formula.
Inline transformations, offsets, dot expansion, and arbitrary calls are not
supported. Create transformed predictors as explicit columns in \code{data}.
}
\seealso{
\code{\link[=JAGS_fit]{JAGS_fit()}} \code{\link[=JAGS_formula]{JAGS_formula()}}
}
