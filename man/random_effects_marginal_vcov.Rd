% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-effects-marginal-covariance.R
\name{random_effects_marginal_vcov}
\alias{random_effects_marginal_vcov}
\title{Random-effect marginal variance-covariance samples}
\usage{
random_effects_marginal_vcov(
  fit,
  parameter = NULL,
  data = NULL,
  posterior_samples = NULL,
  prior_list = NULL,
  blocks = NULL,
  new_levels = NULL,
  diagonal_only = FALSE,
  ...
)
}
\arguments{
\item{fit}{a fitted object returned by \code{\link[=JAGS_fit]{JAGS_fit()}}, an object carrying
\code{formula_design} metadata, or a \code{BayesTools_formula_design} object.}

\item{parameter}{formula parameter name. Required when \code{fit} stores more than
one formula design.}

\item{data}{optional data frame defining the observation rows. When \code{NULL},
the fitted formula design rows are used. New random-effect grouping levels
require an explicit \code{new_levels} policy unless allowed by block metadata.
Row-indexed external SD sources require a reconstructing
\code{parameter_source(..., values = ...)} function when \code{data} is supplied.}

\item{posterior_samples}{optional posterior sample matrix, data frame,
\code{mcmc}, or \code{mcmc.list}. When \code{NULL}, samples are extracted from \code{fit}.}

\item{prior_list}{optional named prior list used to materialize fixed point
hyperparameters. When \code{NULL}, priors are taken from \code{fit} or the formula
design.}

\item{blocks}{optional character vector of random-effect block names to
include. The default includes all formula random-effect blocks.}

\item{new_levels}{optional new-level policy. Use a \code{random_new_levels()}
object or one of \code{"error"}, \code{"zero"}, or \code{"sample"}. \code{"zero"} assigns
zero covariance contribution to unseen grouping levels. \code{"sample"} treats
unseen grouping levels as independent draws from the block covariance \code{G}.}

\item{diagonal_only}{whether to return only observation-level marginal
variances. This avoids allocating a \verb{draw x row x row} array when callers
need only \verb{diag(Z G Z')}.}

\item{...}{reserved for future extensions. Unused arguments are rejected.}
}
\value{
A list of class
\code{BayesTools_random_effects_marginal_vcov} with fields:
\describe{
\item{samples}{A dense array with dimensions \verb{draw x row x row}, or a
\verb{draw x row} matrix when \code{diagonal_only = TRUE}.}
\item{metadata}{A list describing row ordering, included blocks,
skipped blocks, structures, dimensions, and memory size. With
\code{diagonal_only = TRUE}, it additionally identifies the \code{"diagonal"}
variance representation, its sample dimensions, and the equivalent
dense entry count. The default covariance metadata schema is
unchanged.}
}
}
\description{
Constructs posterior-draw marginal covariance matrices implied by
BayesTools formula random effects. For each included random-effect block and
posterior draw, the helper builds the observation-level contribution
\eqn{Z_b G_b Z_b'} and sums contributions across blocks.
}
\seealso{
\code{\link[=JAGS_formula_design]{JAGS_formula_design()}} \code{\link[=JAGS_fit]{JAGS_fit()}}
}
