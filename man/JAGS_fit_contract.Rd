% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JAGS-fit-contract.R
\name{JAGS_fit_contract}
\alias{JAGS_fit_contract}
\alias{JAGS_parameter_encode}
\alias{JAGS_parameter_decode}
\alias{JAGS_parameter_encoding_schema}
\alias{JAGS_formula_name_map}
\alias{JAGS_validate_fit_contract}
\alias{JAGS_fit_contract_schema}
\title{JAGS formula parameter encoding and fitted-object contract}
\usage{
JAGS_parameter_encode(fields)

JAGS_parameter_decode(name)

JAGS_parameter_encoding_schema()

JAGS_formula_name_map(fit, parameter = NULL)

JAGS_fit_contract(fit)

JAGS_validate_fit_contract(fit, requires = character())

JAGS_fit_contract_schema()
}
\arguments{
\item{fields}{named list with scalar character fields \code{kind},
\code{formula_parameter}, \code{term}, and \code{role}.}

\item{name}{encoded scalar name returned by \code{JAGS_parameter_encode()}.}

\item{fit}{fitted object created by \code{\link[=JAGS_fit]{JAGS_fit()}}.}

\item{parameter}{optional formula parameter selecting one name map.}

\item{requires}{character vector naming required contract components.}
}
\value{
The encoding helpers return a character scalar or decoded list. The
schema helpers return data frames. \code{JAGS_formula_name_map()} returns a
\code{BayesTools_formula_name_map} data frame (or a named list of them).
\code{JAGS_fit_contract()} returns a named list, and the validator returns it
invisibly.
}
\description{
\code{JAGS_parameter_encode()} creates an injective JAGS-safe semantic identifier
from structured formula-coordinate fields. It does not replace the backend
column name stored in a fit; the persisted formula name map links both names.
\code{JAGS_parameter_decode()} reverses identifiers created by the current
encoding.

\code{JAGS_formula_name_map()} returns the persisted mapping between opaque JAGS
base names and semantic formula coordinates. Downstream code should use this
map or \code{\link[=parameter_coordinates]{parameter_coordinates()}} rather than parsing a JAGS name.

\code{JAGS_fit_contract()} returns the schema contract attached to a fit.
\code{JAGS_validate_fit_contract()} checks the components named in \code{requires} and
requires unsupported objects to be refitted rather than adapted.
}
