% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-parameter-map.R
\name{parameter_map_cache}
\alias{parameter_map_cache}
\title{Cache a value derived from a fitted parameter map}
\usage{
parameter_map_cache(map, provider, key, compute)
}
\arguments{
\item{map}{a parameter map, as returned by \code{\link[=parameter_map]{parameter_map()}}.}

\item{provider}{name of the calling package.}

\item{key}{a value identifying every input other than \code{map} that \code{compute}
depends on. Compared with \code{\link[=identical]{identical()}}.}

\item{compute}{a function of no arguments returning the value to cache.}
}
\value{
The cached or freshly computed value of \code{compute()}.
}
\description{
Stores one value per provider against a fitted parameter map,
for the lifetime of the \R session. It exists so that packages building on
BayesTools can avoid recomputing map-derived metadata without inventing
their own storage inside fitted objects.

The cache is keyed by the map \emph{and} by \code{key}, a value naming everything
else the cached result was derived from. Whenever \code{key} stops being
\code{\link[=identical]{identical()}} to the stored one the value is recomputed, so a result that
also depends on data or priors stays correct when those change. Passing a
\code{key} that does not cover every input is the one way to use this
incorrectly.

Entries never travel with a saved fit, and BayesTools discards every
provider's entries whenever the map's own tables are replaced.
}
\seealso{
\code{\link[=parameter_map]{parameter_map()}}
}
