% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-group-covariance.R
\name{random_group_covariance}
\alias{random_group_covariance}
\title{Known group covariance for random effects}
\usage{
random_group_covariance(covariance, scale = c("cor", "none", "cor0", "cov0"))
}
\arguments{
\item{covariance}{numeric square matrix with row and column names identifying
grouping levels.}

\item{scale}{how to scale the kernel before fitting. \code{"cor"} converts the
reordered matrix to a correlation matrix, \code{"none"} uses the matrix as
supplied, \code{"cor0"} applies \code{(stats::cov2cor(K) - min(K)) / (1 - min(K))},
and \code{"cov0"} applies \code{K - min(K)}.}
}
\value{
A \code{random_group_covariance} object.
}
\description{
Creates a fixed group-level covariance kernel for use with
\code{\link[=random_effects_formula]{random_effects_formula()}}. The kernel is a random-effect design property:
it describes covariance across grouping levels and is separate from
column-level random-effect covariance structures such as
\code{\link[=random_covariance]{random_covariance()}}.
The associated fitted block scale remains the public \code{sd} quantity (with
\code{var} as its square). It multiplies the supplied kernel; it is not an
\code{sd_mult}. Consequently, when an unscaled kernel has a non-unit diagonal,
the fitted \code{sd} need not equal every grouping level's marginal standard
deviation.
}
\seealso{
\code{\link[=random_effects_formula]{random_effects_formula()}}
}
