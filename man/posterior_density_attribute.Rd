% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-density.R
\name{posterior_density_attribute}
\alias{posterior_density_attribute}
\alias{posterior_ordinate_attribute}
\alias{posterior_ordinate_append}
\alias{posterior_ordinate_supports_bf}
\alias{posterior_ordinate_has_value}
\title{Posterior density and ordinate attributes}
\usage{
posterior_density_attribute(
  x,
  y,
  method,
  density_method,
  diagnostics = NULL,
  point_masses = NULL,
  ...
)

posterior_ordinate_attribute(
  value,
  ordinate,
  method,
  density_method,
  diagnostics = NULL,
  ...
)

posterior_ordinate_append(existing, ordinate)

posterior_ordinate_supports_bf(ordinate, validator = NULL)

posterior_ordinate_has_value(ordinate, value)
}
\arguments{
\item{x}{numeric density grid locations.}

\item{y}{numeric density grid heights.}

\item{method}{estimator label stored in the attribute.}

\item{density_method}{public density method label stored in the attribute.}

\item{diagnostics}{optional estimator diagnostics.}

\item{point_masses}{optional point-mass table/list with \code{x} and
\code{mass} entries.}

\item{...}{additional named metadata fields, for example \code{parameter},
\code{conditional}, or \code{conditional_rule}.}

\item{value}{numeric null-hypothesis value or values.}

\item{ordinate}{numeric posterior ordinate height or heights.}

\item{existing}{existing posterior-ordinate attribute or \code{NULL}.}

\item{validator}{optional function for stricter method-specific BF support
checks. It is called only after the generic BayesTools schema is valid.}
}
\value{
\code{posterior_density_attribute()} returns a list suitable for a
\code{posterior_density} attribute.

\code{posterior_ordinate_attribute()} returns a list suitable for a
\code{posterior_ordinate} attribute.

\code{posterior_ordinate_append()} returns a posterior-ordinate
attribute container that may hold multiple ordinates.

\code{posterior_ordinate_supports_bf()} and
\code{posterior_ordinate_has_value()} return scalar logical values.
}
\description{
Construct and inspect posterior-density and posterior-ordinate
attributes in the schema consumed by \code{\link{Savage_Dickey_BF}} and
\code{\link{hypothesis_BF}}.
}
\details{
Additional metadata must be named and cannot replace structural
schema fields. Posterior ordinate values are unique so a point-null lookup
maps to at most one stored ordinate. When a validator is supplied to
\code{posterior_ordinate_supports_bf()}, it is applied to matching ordinate
entries rather than to the multi-ordinate container as a whole.
}
